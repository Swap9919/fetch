// ComponentA.js
import React from 'react';

const ComponentA = ({ value }) => {
      return (
            <div>
                  <h1>Value from ComponentB: {value}</h1>
            </div>
      );
};

export default ComponentA;
